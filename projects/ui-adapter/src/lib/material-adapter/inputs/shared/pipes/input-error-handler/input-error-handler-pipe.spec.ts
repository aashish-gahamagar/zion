import { InputErrorHandlerPipe } from './input-error-handler-pipe';

describe('InputErrorHandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new InputErrorHandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
